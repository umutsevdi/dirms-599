// Re-export all types from feature-based locations
// This file is kept for backward compatibility
// Prefer importing directly from feature folders: `features/{feature}/types/*.types`

export * from "../shared/types";
